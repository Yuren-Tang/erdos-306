# E306-PF-AUDIT-01 — claim matrix

**Frozen candidate:** `5753219dd888b77b0ebe892a8ae7feabb0ca40ae`  
**Classification vocabulary:** exactly the five classes required by research-workbench issue #53.

The classification concerns the frozen mathematical claim, not whether it gives a stronger public theorem. A `VALID STRONGER INTERMEDIATE THEOREM` row strengthens or isolates an internal lemma only; the headline endpoint remains the same.

| ID | Claim and proof location | Independent recomputation | Classification | Gate consequence |
|---|---|---|---|---|
| C1 | Complete-pair reciprocal load, Human Proof §§2–3 | Abel summation gives `S_X=log 3+o(1)` and `U_X=o(1)`; therefore `(S_X^2-U_X)/2 -> (log 3)^2/2 = 0.603474... in (1/3,1)`. | `GENUINE STRICT SIMPLIFICATION` | Sound; removes a selected mass family. |
| C2 | One Bernoulli parameter replaces mass/control normalization, Human Proof §3 | `theta=1/(b Lambda)` gives `theta Lambda=1/b`; `b>=3` and `Lambda>lambda_->1/3` give `theta<1`; `Lambda<1` gives no-wrap. | `GENUINE STRICT SIMPLIFICATION` | Sound. |
| C3 | Elimination of greedy interval, forbidden-pool accounting, `alpha=3/2`, and special load target, Route Comparison §§2,5–6 | None of these objects occurs in the standalone implication chain; the complete pair load supplies both mass and variance. | `GENUINE STRICT SIMPLIFICATION` | Sound; headline theorem unchanged. |
| C4 | Probabilistic concentration as a replacement route, Route Comparison §3 | Exact selection is a lattice local-limit problem; characteristic-function control at all frequencies is the same finite Fourier task. | `EQUIVALENT REPARAMETERIZATION` | Correctly rejected as an independent simplification. |
| C5 | One abstract CRT tail theorem packages hot/cold/encoding/tails, Route Comparison §4; Architecture Layer III | The theorem is exactly the interface later consumed by the Fourier minor estimate; its proof still contains all local ingredients. | `EQUIVALENT REPARAMETERIZATION` | Good hierarchy, not new endpoint strength. |
| C6 | Removing all `b`-sensitive auxiliary denominators, Route Comparison D3 | Pair denominators determine only the residue modulo the block-prime product `P`; siblings differing only modulo `b` would be invisible. | `FALSE` | Correctly not adopted. |
| C7 | One common reservoir works for every sibling, Route Comparison D4; Human Proof §14 | A sibling has at least one mismatch prime `r|b`; the same `G` high primes yield `G` denominators `rs` and the same damping estimate. | `GENUINE STRICT SIMPLIFICATION` | Sound. |
| R1 | Deterministic reciprocal dispersion, Lemma 7 | Close residues correspond to `O(s)` small integers `v`; each gives at most two primes in `[X,2X)`. At least `s/2` terms exceed `s/(64X)`. | `VALID STRONGER INTERMEDIATE THEOREM` | Sound for `s>=16`. |
| R2 | Cross-label energy, Lemma 8 | The proof is correct only when the first class has at least sixteen vertices. The printed hypothesis `|C_m|>=128B/X` alone permits smaller classes. | `FALSE` | Mandatory Repair R1: add `|C_m|>=max(16,128B/X)` or restrict the lemma to substantial classes. |
| R3 | Bad-pair average and base prime, Proposition 9 | Bad degree average is `2#bad/N`; substituting `B=A sqrt(R)X^2/N` gives `O(N/A^2)`. | `VALID STRONGER INTERMEDIATE THEOREM` | Sound for `R>0`; zero case must be separated. |
| R4 | Label-cover count, Proposition 9 | All labels lie in `[-B,B]` and one residue class modulo `p_0>=X`, hence at most `2B/X+2`. | `VALID STRONGER INTERMEDIATE THEOREM` | Sound. |
| R5 | Small-class branch of nondominant forcing, Proposition 9 | `rho N/4 <= M s_0 <= C(B/X+1)^2`; if `B/X>=1`, substitution yields `R >= cN^3/X^2 >= cX/log^3X`. | `VALID STRONGER INTERMEDIATE THEOREM` | Sound. |
| R6 | Substantial-class branch of nondominant forcing, Proposition 9 | The printed sum must be `(S-n_i)-(t-1)`, with a factor `1/2` if ordered pairs are used. Since every substantial class has size at least `s_0>=256`, the subtraction is absorbed by `255/256`; the power-mean algebra then closes. | `UNPROVED / GAP` | Mandatory Repairs R2–R3; same route and conclusion survive. |
| R7 | Proposition 9 as a whole | After separating `R=0`, correcting Lemma 8, and correcting the class sum, both alternatives force `R>=cX/log^3X`. | `UNPROVED / GAP` | Bounded local repair only. |
| R8 | Zero-exception cold blocks, Proposition 10 | Dominant-label energy bounds `m`; each exception contributes `cN^3/X^2` on disjoint edges; `|E|<Cc_w<1`. | `VALID STRONGER INTERMEDIATE THEOREM` | Valid after Proposition 9 is repaired; stronger intermediate lemma, not stronger headline theorem. |
| F1 | Fingerprint uniqueness, Lemma 11 | Two candidate residues would make the dispersion sum at most twice their energies; choosing `g<c_D/4` forbids this. | `VALID STRONGER INTERMEDIATE THEOREM` | Sound. |
| F2 | Fingerprint size and exception count in lower energy range, Proposition 12 | `s~(RX^2)^(1/4)` and `t<=RX^2/(gs^3)=O(s)`; assignment entropy is `O((RX^2)^(1/4)log X)=o(R)` at the forcing floor. | `VALID STRONGER INTERMEDIATE THEOREM` | Sound and uniform in `R`. |
| F3 | Trivial high-energy entropy range, Proposition 12 | Total logarithmic entropy is `O(N log X)=O(X)` while `R>>N^4/X^2>>X^2/log^4X`. | `VALID STRONGER INTERMEDIATE THEOREM` | Sound. |
| A1 | Adjacent cold labels pay `Pi_k`, Lemma 13 | Cold label bounds give `|d|<4X^2`; at most one next-block prime divides `d`; dispersion over all other primes gives `Pi_k>>2^(2k)/k^4`. | `VALID STRONGER INTERMEDIATE THEOREM` | Sound. |
| G1 | Global code is injective, Proposition 14 | Shell vector + hot set + boundary set + complete hot assignments + one label per cold segment reconstruct every coordinate. | `VALID STRONGER INTERMEDIATE THEOREM` | Sound. |
| G2 | Shell count, Proposition 14 | The generating-function bound `e^(delta R)(1-e^-delta)^(-J)` follows by weighting every vector with `sum r_k<=R`. | `EQUIVALENT REPARAMETERIZATION` | Sound. |
| G3 | Hot-block count, Proposition 14 | For hot `k`, `r_k+1>=w_k`, so Proposition 12 applies with a uniform scale threshold and contributes `e^(delta(r_k+1))`. | `VALID STRONGER INTERMEDIATE THEOREM` | Sound. |
| G4 | Later segment-label costs are paid by triggers, Proposition 14 | `log(1+C/sigma_s)=O(s)`; both `w_(s-1)` and `Pi_(s-1)` eventually dominate `s/delta`. Each trigger begins at most one segment; internal and boundary edge families are disjoint. | `VALID STRONGER INTERMEDIATE THEOREM` | Sound; no double charging that changes the exponent class. |
| L1 | Forcing floor is the bottom hot threshold, §11 | `w_k` is eventually increasing; `Pi_k>>2^(2k)/k^4` and `Pi_n/w_n>>X/n`, so `F_0=w_n>>X/n^3`. | `VALID STRONGER INTERMEDIATE THEOREM` | Sound. |
| L2 | Exact diagonal identity and unique label, Lemma 15 | Below `F_0` all blocks are cold with one label; `|m|=o(X^2)` makes every centred lift equal `m`, giving `Q=m^2sigma^2`; two labels would differ by two bottom primes. | `VALID STRONGER INTERMEDIATE THEOREM` | Sound. |
| L3 | High-floor Laplace domination, Proposition 16 | Level-set entropy is `e^(A J+gamma R)` and `F_0/J>>2^n/n^4 -> infinity`, so the tail is at most `eta/sigma`. | `VALID STRONGER INTERMEDIATE THEOREM` | Sound. |
| L4 | Diagonal Gaussian tail, Proposition 16 | Split `e^(-cm^2sigma^2)` in half; outside `C/sigma` one half gives `e^(-cC^2/2)` and the full remaining Gaussian is `O_c(1/sigma)`. | `EQUIVALENT REPARAMETERIZATION` | Sound. |
| V1 | Full-pair inverse-square mass is comparable with scaffold variance, §12 | Pair square mass is at most `U_X^2/2=O((nX)^-2)`; the bottom internal block is `Omega((nX)^-2)`. | `GENUINE STRICT SIMPLIFICATION` | Sound; no large variance certificate needed. |
| V2 | Auxiliary variance is absorbed without circularity in `G`, §12 | For fixed `G`, auxiliary square mass is `O_(b,G)(X^-6)=o((nX)^-2)`. Choose the comparison constant first and only its threshold after `G`. | `GENUINE STRICT SIMPLIFICATION` | Sound. |
| M1 | Uniform logarithmic Taylor expansion, §13 | Compactness of the `theta` interval gives uniform radius and cubic constant; `max|m/e|=O_C(n/X)` and total remainder `O_(b,C)(n/X)`. | `VALID STRONGER INTERMEDIATE THEOREM` | Sound. |
| M2 | Positive major lower bound, §13 | Exact cancellation removes the linear phase; a small complex remainder gives positive real part; `|m|<=1/(2sigma_E)` supplies `Omega(1/sigma_E)` terms of bounded Gaussian size. | `VALID STRONGER INTERMEDIATE THEOREM` | Sound. |
| m1 | Exact block-minor fibre factor `b`, §14 | CRT fixes one residue modulo `P`; because `(b,P)=1`, exactly `b` residues modulo `bP` lift it. | `EQUIVALENT REPARAMETERIZATION` | Sound; factor remains visible. |
| m2 | Squarefree mismatch-prime argument, §14 | Agreement modulo every prime divisor of squarefree `b` implies agreement modulo `b`; a nongenuine sibling therefore mismatches at some `r|b`. | `VALID STRONGER INTERMEDIATE THEOREM` | Sound; this is the second genuine use of squarefreeness. |
| m3 | Common-reservoir damping and count `b(2N+1)`, §14 | `h=m+js`, `(r,s)=1`, and mismatch gives `r∤j`; with `s>2N`, distance is at least `1/(2r)`. There are at most `2N+1` labels and `b` lifts each. | `GENUINE STRICT SIMPLIFICATION` | Sound. |
| T1 | Parameter order `eta -> C -> G -> n`, §15 and ledger | `K_b,c_maj,c_b,beta_b` precede terminal choices; `K_c` is independent of `eta`; the final scale absorbs PNT, rigidity, reservoir, Taylor, and variance thresholds. | `VALID STRONGER INTERMEDIATE THEOREM` | Sound and non-circular. |
| T2 | Strict Fourier positivity, §15 | After multiplication by `sigma`, the block high-floor, diagonal Gaussian, and sibling budgets are each below one sixth of the major allowance; their sum is below one half. | `EQUIVALENT REPARAMETERIZATION` | Sound. |
| T3 | No-wrap conversion, §15 | The congruence difference is an integer in `(-1,1)` because the selected sum lies in `[0,Lambda]` with `Lambda<1` and `1/b in (0,1)`. | `GENUINE STRICT SIMPLIFICATION` | Sound. |
| H1 | Avoidance, distinctness, numerator induction, and `b=1,2` closure, §§1,15 | Unique factorization and scale avoid `T`; successive forbidden sets prevent collisions; the elementary identities reduce the two small denominators. | `EQUIVALENT REPARAMETERIZATION` | Sound. |

## Matrix conclusion

The only false adopted statement is Lemma 8 in an unnecessarily broad small-class range; every actual use satisfies the missing hypothesis. The only unresolved adopted proof node is Proposition 9 as printed, because of that hypothesis, the unhandled `R=0` division, and the cross-class summation notation. The corrected inequalities close with the existing thresholds and constants. All later route-critical claims survive unchanged.

`[PASS-WITH-BOUNDED-REPAIRS E306-PF-AUDIT-01]`
