name: verify-e306-manuscript

on:
  pull_request:
    paths:
      - "paper/e306-anchor-fibre-v1/manuscript/**"
      - ".github/workflows/manuscript.yml"
  workflow_dispatch:

jobs:
  build-manuscript:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: paper/e306-anchor-fibre-v1/manuscript

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Verify frozen manuscript source
        run: |
          git diff --exit-code dba5400f67acc2a8c05dedc1a1861700e17ee33d -- \
            main.tex references.bib sections

      - name: Guard Oxford manuscript spelling
        run: |
          python3 - <<'PY'
          import re
          from pathlib import Path

          files = [Path("main.tex"), *sorted(Path("sections").glob("*.tex"))]
          forbidden = {
              r"\bcentered\b": "centred",
              r"\bfibers?\b": "fibre/fibres",
              r"\banalyz(?:e|ed|es|ing)\b": "analyse/analysed/analyses/analysing",
              r"\bbehavior(?:s|al)?\b": "behaviour...",
              r"\blabel(?:ed|ing)\b": "labelled/labelling",
              r"\bmodel(?:ed|ing)\b": "modelled/modelling",
              r"\bcharacteris(?:e|ed|es|ing|ation|ations)\b": "characteriz...",
              r"\bnormalis(?:e|ed|es|ing|ation|ations)\b": "normaliz...",
              r"\borganis(?:e|ed|es|ing|ation|ations)\b": "organiz...",
              r"\brealis(?:e|ed|es|ing|ation|ations)\b": "realiz...",
              r"\brecognis(?:e|ed|es|ing|ation|ations)\b": "recogniz...",
              r"\bstandardis(?:e|ed|es|ing|ation|ations)\b": "standardiz...",
              r"\boptimis(?:e|ed|es|ing|ation|ations)\b": "optimiz...",
              r"\bminimis(?:e|ed|es|ing|ation|ations)\b": "minimiz...",
              r"\bmaximis(?:e|ed|es|ing|ation|ations)\b": "maximiz...",
              r"\bsummaris(?:e|ed|es|ing|ation|ations)\b": "summariz...",
          }
          bad = []
          for path in files:
              text = path.read_text(encoding="utf-8")
              for pattern, preferred in forbidden.items():
                  for match in re.finditer(pattern, text, flags=re.IGNORECASE):
                      line = text.count("\n", 0, match.start()) + 1
                      bad.append(f"{path}:{line}: {match.group(0)!r} -> prefer {preferred}")
          if bad:
              raise SystemExit("Oxford-spelling guard failed:\n" + "\n".join(bad))
          PY

      - name: Install TeX toolchain
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            latexmk \
            biber \
            poppler-utils \
            tex-gyre \
            fonts-texgyre \
            fonts-gfs-porson \
            fonts-inconsolata \
            texlive-bibtex-extra \
            texlive-fonts-extra \
            texlive-fonts-recommended \
            texlive-latex-extra \
            texlive-plain-generic \
            texlive-xetex

      - name: Verify required font families
        run: |
          fc-match "TeX Gyre Pagella" | tee /tmp/pagella-match.txt
          fc-match "GFS Porson" | tee /tmp/porson-match.txt
          fc-match "Inconsolata" | tee /tmp/inconsolata-match.txt
          grep -qi "pagella" /tmp/pagella-match.txt
          grep -qi "porson" /tmp/porson-match.txt
          grep -qi "inconsolata" /tmp/inconsolata-match.txt

      - name: Clean XeLaTeX and Biber build
        shell: bash
        run: |
          set -o pipefail
          make clean
          make pdf 2>&1 | tee build-output.log

      - name: Extract PDF text
        if: always()
        run: |
          if test -s main.pdf; then
            pdftotext -enc UTF-8 main.pdf main.txt
          fi

      - name: Upload build diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: e306-manuscript-diagnostics-${{ github.sha }}
          path: |
            paper/e306-anchor-fibre-v1/manuscript/build-output.log
            paper/e306-anchor-fibre-v1/manuscript/main.log
            paper/e306-anchor-fibre-v1/manuscript/main.blg
            paper/e306-anchor-fibre-v1/manuscript/main.txt
          if-no-files-found: warn
          retention-days: 30

      - name: Gate bibliography, references, and labels
        run: |
          test -s main.pdf
          test -s main.bbl
          if grep -E "LaTeX Warning: (There were undefined references|Citation .* undefined|Reference .* undefined|Label .* multiply defined)" main.log; then
            echo "::error::unresolved or duplicate references remain"
            exit 1
          fi
          if grep -E "There were undefined references|undefined citations|multiply-defined labels" main.log; then
            echo "::error::unresolved or duplicate references remain"
            exit 1
          fi
          if grep -E "(^|[[:space:]])(WARN|ERROR) -" main.blg; then
            echo "::error::Biber reported a warning or error"
            exit 1
          fi

      - name: Verify embedded fonts, Unicode, and extracted identity text
        run: |
          pdffonts main.pdf | tee main-fonts.txt
          if grep -q "Type 3" main-fonts.txt; then
            echo "::error::Type 3 fonts are not permitted"
            exit 1
          fi
          awk 'NR > 2 && $(NF-4) != "yes" { bad=1 } END { exit bad }' main-fonts.txt
          python3 - <<'PY'
          import re
          import unicodedata
          from pathlib import Path
          text = unicodedata.normalize("NFC", Path("main.txt").read_text(encoding="utf-8"))
          required = [
              "τοῖς ἐμὲ φιλοῦσιν",
              "καὶ οἷς φιλῶ",
              "https://orcid.org/0009-0006-0847-3330",
          ]
          missing = [s for s in required if s not in text]
          if missing:
              raise SystemExit("Required Unicode/identity extraction failed: " + repr(missing))
          leaked = sorted(set(re.findall(r"\b(?:eq|thm|sec|lem|prop):[A-Za-z0-9_.:-]+", text)))
          if leaked:
              raise SystemExit("Internal LaTeX label names leaked into PDF text: " + repr(leaked))
          PY

      - name: Create minimal arXiv source archive
        run: |
          python3 - <<'PY'
          from pathlib import Path
          from zipfile import ZIP_DEFLATED, ZipFile, ZipInfo

          frozen = "dba5400f67acc2a8c05dedc1a1861700e17ee33d"
          files = [Path("main.tex"), Path("references.bib"), *sorted(Path("sections").glob("*.tex"))]
          expected = 12
          if len(files) != expected:
              raise SystemExit(f"expected {expected} arXiv source files, found {len(files)}")
          out = Path(f"e306-arxiv-v1-source-{frozen[:12]}.zip")
          with ZipFile(out, "w", compression=ZIP_DEFLATED, compresslevel=9) as zf:
              for path in files:
                  data = path.read_bytes()
                  info = ZipInfo(path.as_posix(), date_time=(2026, 8, 7, 0, 0, 0))
                  info.compress_type = ZIP_DEFLATED
                  info.external_attr = 0o100644 << 16
                  zf.writestr(info, data)
          print(out)
          with ZipFile(out) as zf:
              names = zf.namelist()
          if names != [p.as_posix() for p in files]:
              raise SystemExit("archive manifest mismatch")
          forbidden_suffixes = {".pdf", ".bbl", ".aux", ".log", ".blg", ".otf", ".ttf"}
          if any(Path(name).suffix.lower() in forbidden_suffixes for name in names):
              raise SystemExit("forbidden generated/font file entered arXiv archive")
          PY

      - name: Upload minimal arXiv source archive
        uses: actions/upload-artifact@v4
        with:
          name: e306-arxiv-v1-source-${{ github.sha }}
          path: paper/e306-anchor-fibre-v1/manuscript/e306-arxiv-v1-source-dba5400f67ac.zip
          if-no-files-found: error
          retention-days: 90

      - name: Build summary
        run: |
          {
            echo "## E306 manuscript build"
            echo
            echo "- Commit: $GITHUB_SHA"
            echo "- Frozen manuscript source: \`dba5400f67acc2a8c05dedc1a1861700e17ee33d\`"
            echo "- Entry point: \`paper/e306-anchor-fibre-v1/manuscript/main.tex\`"
            echo "- Command: \`make clean && make pdf\`"
            echo "- Engine: XeLaTeX"
            echo "- Bibliography backend: Biber"
            echo "- Result: frozen-source check, Oxford-spelling guard, clean PDF build, bibliography/reference/label gate, font-embedding gate, Unicode/ORCID extraction gate, leaked-label gate, and minimal arXiv source packaging passed"
            echo "- PDF size: $(stat -c%s main.pdf) bytes"
            echo "- Pages: $(pdfinfo main.pdf | awk '/^Pages:/ {print $2}')"
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Upload manuscript PDF
        uses: actions/upload-artifact@v4
        with:
          name: e306-manuscript-${{ github.sha }}
          path: paper/e306-anchor-fibre-v1/manuscript/main.pdf
          if-no-files-found: error
          retention-days: 30
