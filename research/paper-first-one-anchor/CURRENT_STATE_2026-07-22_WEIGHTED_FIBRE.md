# E306-RL paper-first one-anchor current state — sparse synchronization checkpoint

**Date:** 2026-07-23  
**Programme:** `E306-RL`  
**Authority:** research only; not independently reviewed

## Exact source sequence

1. `PAPER_FIRST_ONE_ANCHOR_ROUTE_AUDIT_ADDENDUM_01.md`  
   all-coordinate strengthening and removal of the separate sibling sector;
2. `PAPER_FIRST_ONE_ANCHOR_ROUTE_AUDIT_ADDENDUM_02.md`  
   weighted-fibre normalization repair and exhaustive Fourier ledger;
3. `PAPER_FIRST_ONE_ANCHOR_MAJOR_ARC_AND_TERMINAL_BUDGET.md`  
   direct full-family major arc and strict terminal budget;
4. `SPARSE_TOP_SYNCHRONIZATION_CORE_V1.md`  
   polylogarithmic conditional sensor core and terminal-core obstruction;
5. `SPARSE_ARITHMETIC_SYNCHRONIZATION_CLUSTER_EXPANSION_V1.md`  
   decoder-defect expansion resolving the sparse synchronization frontier.

## Current mathematical classification

```text
MATERIAL STRICTER ALTERNATIVE ROUTE CANDIDATE /
ROUTE-LEVEL FRONTIER AHEAD OF CURRENT RECORDED E306 PROOF DAGS /
WEIGHTED-FIBRE NORMALIZATION REPAIRED /
DIRECT FULL-FAMILY MAJOR ARC CLOSED /
SPARSE ARITHMETIC SYNCHRONIZATION SOLVED AT ARGUMENT LEVEL /
FINGERPRINT ENTROPY NO LONGER LOAD-BEARING /
NOT INDEPENDENTLY REVIEWED.
```

The independently audited complete-pair multiblock route remains the assured
fallback.  Its verdict is `PASS-WITH-BOUNDED-REPAIRS`; it is not altered by this
research checkpoint.

## Denominator architecture

Let

```text
P={p prime:X<=p<X^3},
Z=X^3,
B={q prime:Z/2<=q<Z},
E_pair={pq:p,q in P,p<q}.
```

Choose one actual-prime core

```text
S subset B,
|S|=O((log Z)^3),
```

with row distance

```text
D_r(S)>=A log Z
```

simultaneously for every prime `X<=r<Z` and every nonzero row difference.  For the
prime factors of the fixed squarefree target denominator use only

```text
E_b^S={rs:r|b prime,s in S}.
```

Its loads are

```text
Lambda_b^S=O_b((log Z)^3/Z),
W2_b^S=O_b((log Z)^3/Z^2),
```

so it is negligible in load, variance, and Taylor estimates.  No PNT in arithmetic
progressions is required.

## Decoder-defect synchronization

Put `R=B\S`.  For a fixed core assignment `y`, let `x_r^*(y)` be the nearest row
decoder and `e_r(y)` its star energy.  Expand the top partition by

```text
U={r in R:x_r!=x_r^*(y)},
G=R\U.
```

The retained-energy expansion is

```text
Z_R(y)
 <= sum_(U subset R)
    delta^|U|
    exp(-kappa_b T_U(y)),
```

where

```text
T_U(y)=sum_(r in G)e_r(y)+Q_G(x^*(y)|_G),
delta<=Z^(-K).
```

Every term retains the complete-pair energy on the nondefective witness rows.
This is the correction which prevents the quotient entropy from multiplying an
unweighted additive fibre error.

If `|U|<=|R|/2`, then `G` is a positive-density top block.  Robust deterministic
exact-cold rigidity gives one common integer label `m` on `G` whenever
`Q_G<<Z/(log Z)^3`.  A core coordinate differing from `m` then pays

```text
>>Z/(log Z)^3
```

across the `S x G` sensor edges.  Hence retained energy below a sufficiently small
multiple of `Z/(log Z)^3` forces the whole core and all retained witness rows onto
one short integer diagonal.

For off-diagonal assignments with small `U`, the synchronization floor beats the
core entropy

```text
log product_(s in S)s=O((log Z)^4).
```

For `|U|>|R|/2`, the factor `delta^|U|` gives `exp(-Omega(Z))` suppression.
Therefore

```text
top off-diagonal partition
 =exp(-Omega_b(Z/(log Z)^3)).
```

The surviving diagonal partition has Gaussian scale

```text
sigma_top^2 asymp 1/(Z^2(log Z)^2),
partition <<1/sigma_top<<Z log Z.
```

Thus the sparse quotient is normalized without the one-block fingerprint-entropy
theorem.

## Direct actual-family major arc

For the complete denominator family

```text
sigma_E^2=theta(1-theta)sum_(e in E)1/e^2
          asymp_b 1/(X^2(log X)^2).
```

Uniformly for `|m|<=C/sigma_E`,

```text
sum_e|m/e|^3=O_(b,C)(log X/X)=o(1).
```

The exact identity `theta sum_e1/e=1/b` cancels the linear phase, so

```text
log F(m)=-2pi^2m^2sigma_E^2+o_(b,C)(1),
Re sum_(|m|<=C/sigma_E)F(m)>=c_maj/sigma_E.
```

No scaffold variance, comparison constant, fingerprint entropy, or fixed
reservoir size is needed.

## Terminal lanes

1. genuine major frequencies `|m|<=C/sigma_E`;
2. full-variance Gaussian tail up to `X^2/4`;
3. adaptive complete-pair damping up to `M_dec=XZ/(log Z)^2`;
4. large synchronized integer labels;
5. sparse-synchronization off-diagonal sector;
6. all lower-prime and target-denominator nondecoder fibres.

The asymptotic terminal order remains

```text
C -> X,
```

after fixing the sensor-strength constant from the compact Bernoulli interval.

## Removed proof layers

The proposed proof DAG no longer uses:

```text
all-block hot/cold bookkeeping,
adjacent-label propagation,
global injective encoding,
global level-set/localization,
separate sibling sector,
fingerprint entropy as a load-bearing theorem,
eta,
fixed reservoir size G.
```

The only surviving dense local rigidity input is the deterministic zero-exception
exact-cold theorem on a positive-density witness block.  Its edges already belong
to the natural complete-pair denominator family.

## Assurance disposition

Remaining work is now proof-development and independent assurance:

1. write the robust positive-density-subset exact-cold theorem with the bounded
   repairs from `E306-PF-AUDIT-01`;
2. verify the exact kernel exponent and decoder-defect expansion constants;
3. audit defect amplification and label-size inequalities;
4. integrate the synchronization theorem with lower-coordinate fibres and the
   closed major/minor Fourier ledger;
5. independently audit the assembled shorter route.

Recommended control flow:

```text
PDL route expansion
-> independent one-anchor/sparse-synchronization audit
-> DIR comparison against the assured multiblock fallback
-> only then manuscript or MATH-CUR intake.
```

RL returns to further new mathematics and receives back only an exact failed
implication or structural obstruction.

## Current frontier classification

```text
POLYLOG ACTUAL-PRIME SENSOR CORE — PROVED ARGUMENT
DECODER-DEFECT EXPANSION — PROVED ARGUMENT
SPARSE ARITHMETIC SYNCHRONIZATION — ARGUMENT-LEVEL SOLUTION
FINGERPRINT ENTROPY — NOT LOAD-BEARING IN THE NEW ROUTE
FULL SHORTER ROUTE — MATERIAL CANDIDATE / HANDED TO ASSURANCE
RELEASED THEOREM STATUS — UNCHANGED.
```

No manuscript, Curator, review, Lean, release, DOI, arXiv, or canonical status
changes follow from this checkpoint.
